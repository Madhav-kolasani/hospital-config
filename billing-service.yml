server:
  port: 6607

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/billing_db
    username: root
    password: MySQL@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
